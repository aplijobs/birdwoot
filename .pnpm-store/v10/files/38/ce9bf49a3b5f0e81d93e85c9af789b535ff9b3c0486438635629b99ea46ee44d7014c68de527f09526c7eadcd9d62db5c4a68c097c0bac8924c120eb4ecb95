import { Client } from '@sentry/core';
import { Options, TracingOptions } from './types';
/**
 * Inits the Vue SDK
 */
export declare function init(config?: Partial<Pick<Options, Exclude<keyof Options, 'tracingOptions'>> & {
    /**
     * @deprecated Add the `vueIntegration()` and pass the `tracingOptions` there instead.
     */
    tracingOptions: Partial<TracingOptions>;
}>): Client | undefined;
//# sourceMappingURL=sdk.d.ts.map
